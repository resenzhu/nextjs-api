import Mailjet from 'node-mailjet';

export const sendEmail = ({name, email, message}: {name: string, email: string, password: string}): Promise<void> => (
  new Promise((resolve, reject): void => {})
);
